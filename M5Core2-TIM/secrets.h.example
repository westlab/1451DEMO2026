// secrets.h.example --- copy to secrets.h and fill in, then keep secrets.h
// out of version control (.gitignore already excludes it).

#pragma once

#define WIFI_SSID  "your-wifi-ssid"
#define WIFI_PASS  "your-wifi-password"

// Optional broker override. The sketch defaults to the LAN broker 10.42.0.1
// (offline demo); uncomment to point somewhere else:
// #define MQTT_HOST  "10.42.0.1"
// #define MQTT_PORT  1883
